/**
 * Sectors / Trip tab route — immersive map layout (active).
 * Bottom tab label is "Trip"; route group stays `(sectors)`.
 *
 * Roll back to classic: change the export below to
 * `export { default } from "@/components/sectors/SectorsScreen.classic";`
 */
export { default } from "@/components/sectors/SectorsScreen";
