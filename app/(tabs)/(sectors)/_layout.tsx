import { Stack } from "expo-router";
import React from "react";

export default function SectorsStackLayout() {
  //return <Stack screenOptions={{ headerShown: false }} />;
  return <Stack />; // Native title headers so <Header /> properties function properly
}
